/** The operational intent of an invocation (Section 2) — orthogonal to `Target`. Affects release-locator sourcing and whether watch is active (Section 12, reserved). */
export type Mode = "development" | "production";
