# `ens init`

Scaffold a new Ensemble project.

```sh
ens init
```

Prompts for a project name, then lays down the `source/` and `.ensemble/`
folders described in [Workspace layout](../../concepts/workspace-layout.md),
fetches the built-in kits, and initializes a git repository.

No flags or arguments — the project name is entered interactively.
