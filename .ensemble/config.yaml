build:
  cli:
    kit: deno.bundle
  demo/spa:
    kit: react
  website/content:
    kit: react
    target: ssr
  website/server:
    kit: deno.bundle
hooks:
  release:
    after:
      - name: changelog
        run: >-
          RUST_LOG=off git-cliff --config .ensemble/configs/cliff.toml --latest
          --prepend CHANGELOG.md && { git diff --quiet -- CHANGELOG.md || git
          commit CHANGELOG.md -m "chore(changelog): update for $ENSEMBLE_RELEASE_TAG"; }
