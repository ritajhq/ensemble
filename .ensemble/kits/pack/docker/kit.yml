modes:
  image: image,push=false
  tar: tar
  oci: oci
  local: local,mode=delete
publish:
  # Option values (registry, …) come from each release's publish: block.
  push: "Tag and push the image to a container registry. Options: registry."
