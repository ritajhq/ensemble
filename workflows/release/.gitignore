# Secrets (GH_TOKEN, REGISTRY_USERNAME, REGISTRY_PASSWORD) live encrypted
# in contexts/<name>/secrets.yml, which IS committed — see "ens workflow
# secrets edit" and @ensemble/workflow's context-loaders/secrets-crypto.ts.
